name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test & Build
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: ".python-version"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Sync Python dependencies
        run: uv sync --locked

      - name: Typecheck
        run: pnpm typecheck

      - name: Run Python tests
        run: uv run pytest tests/python/test_lora_manager_to_image_saver_hashes.py -q

      - name: Run tests
        run: pnpm test

      - name: Build
        run: pnpm build
